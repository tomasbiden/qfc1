# 门票秒杀系统结项答辩｜第一章：需求分析

这是独立搭建的 Slidev 工程，当前共 9 页。旧版工程未修改。

```bash
npm install
npm run dev
```

开发命令已启用轮询监听，避免 macOS 上文件监听数量过多导致 `EMFILE`。

构建和导出：

```bash
npm run build
npm run export:pdf
npm run export:pptx
```
